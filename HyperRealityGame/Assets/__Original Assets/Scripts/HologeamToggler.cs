using UnityEngine;
using System.Collections;

public class HologeamToggler : MonoBehaviour
{
    private bool on;
    public GameObject holograms;

	void Update () {
        /*
        if(Input.GetKeyDown(KeyCode.Z) ||
            ArduinoManager.instance.GetButtonDownState(2))
        {
            if (!holograms.activeSelf)
            {
                holograms.SetActive(true);
            }
            else
            {
                holograms.SetActive(false);
            }
        }
        */
    }
}
